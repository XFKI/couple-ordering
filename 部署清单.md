# PWA部署完成清单 ✅

## 已完成的配置

### 1. PWA核心文件
- ✅ `public/manifest.json` - PWA配置清单
- ✅ `public/sw.js` - Service Worker离线缓存
- ✅ `index.html` - 添加了PWA元标签和SW注册

### 2. 构建配置
- ✅ `vite.config.js` - 配置GitHub Pages部署
- ✅ `package.json` - 添加部署脚本和gh-pages依赖

### 3. Git配置
- ✅ `.gitignore` - 忽略不必要的文件

---

## 🚀 立即部署步骤

### 第一步：安装部署工具
```bash
npm install
```

### 第二步：测试构建
```bash
npm run build
```
如果成功，会生成 `dist` 文件夹

### 第三步：创建GitHub仓库

**重要**：根据你的仓库类型选择base配置

#### 选项A：部署到项目仓库（推荐）
仓库名例如：`couple-ordering`

修改 `vite.config.js` 第7行：
```javascript
base: '/couple-ordering/'  // 改为你的仓库名
```

访问地址：`https://你的用户名.github.io/couple-ordering/`

#### 选项B：部署到用户主页
仓库名必须是：`你的用户名.github.io`

保持 `vite.config.js` 第7行：
```javascript
base: '/'
```

访问地址：`https://你的用户名.github.io/`

### 第四步：初始化Git
```bash
git init
git add .
git commit -m "PWA初始版本"
git branch -M main
git remote add origin https://github.com/你的用户名/仓库名.git
git push -u origin main
```

### 第五步：部署
```bash
npm run deploy
```

### 第六步：启用GitHub Pages
1. 访问仓库 → Settings → Pages
2. Source 选择 `gh-pages` 分支
3. 点击Save
4. 等待2-5分钟部署完成

---

## 📱 还需要做的事情

### 1. 添加PWA图标（必需）

**快速方法 - 使用在线工具**：
1. 访问 https://realfavicongenerator.net/
2. 上传一张图片（建议1024x1024）
3. 下载生成的图标包
4. 将 `icon-192.png` 和 `icon-512.png` 放到 `public/` 文件夹

**简易方法 - 使用Emoji**：
1. 在网上搜索 "🍽️ emoji png download"
2. 下载PNG格式
3. 调整为192x192和512x512
4. 重命名并放到 `public/` 文件夹

**临时图标（测试用）**：
现在可以不添加图标，部署后会显示默认图标

### 2. 测试PWA功能

部署成功后：
1. 手机打开网站
2. 点击"添加到主屏幕"
3. 测试离线功能（关闭WiFi和流量，仍能打开应用）

---

## 🔄 日常更新流程

修改代码后重新部署：
```bash
git add .
git commit -m "更新说明"
git push
npm run deploy
```

---

## 💡 提示

1. **首次部署需要时间**：GitHub Pages需要5-10分钟才能访问
2. **图标可以后续添加**：先测试部署成功，图标慢慢优化
3. **Supabase配置不要提交**：如果想开源，记得隐藏API密钥
4. **测试多个浏览器**：Chrome、Safari都测试一下

---

## ❓ 遇到问题？

### 问题1：npm run deploy失败
**解决**：检查是否安装了gh-pages
```bash
npm install gh-pages --save-dev
```

### 问题2：GitHub Pages显示404
**解决**：检查vite.config.js的base路径是否正确

### 问题3：部署后页面空白
**解决**：
- 检查浏览器控制台错误
- 确认base路径配置
- 清除浏览器缓存

### 问题4：Service Worker不生效
**解决**：
- 必须使用HTTPS（GitHub Pages自动提供）
- 打开浏览器开发者工具 → Application → Service Workers 检查状态

---

**准备好了吗？开始部署吧！** 🎉

有问题随时问我！
