# 🚀 PWA部署指南

## 📋 部署到GitHub Pages

### 第一步：安装部署工具
```bash
npm install gh-pages --save-dev
```

### 第二步：创建GitHub仓库
1. 访问 https://github.com/new
2. 仓库名：`couple-ordering` (或你喜欢的名字)
3. 设为Public
4. 点击Create repository

### 第三步：配置base路径
如果你的仓库名是 `couple-ordering`，修改 `vite.config.js`:
```javascript
base: '/couple-ordering/'  // 改为你的仓库名
```

如果部署到 `username.github.io` (主页)，保持:
```javascript
base: '/'
```

### 第四步：初始化Git并推送
```bash
# 初始化Git仓库
git init
git add .
git commit -m "Initial commit"

# 连接到GitHub仓库
git remote add origin https://github.com/你的用户名/couple-ordering.git

# 推送到GitHub
git branch -M main
git push -u origin main
```

### 第五步：部署
```bash
npm run deploy
```

### 第六步：启用GitHub Pages
1. 访问你的仓库 → Settings → Pages
2. Source 选择: `gh-pages` 分支
3. 点击Save
4. 等待几分钟

### 第七步：访问你的网站
```
https://你的用户名.github.io/仓库名/
```

---

## 📱 添加到手机桌面

### iPhone (Safari)
1. 打开网站
2. 点击分享按钮 (⬆️)
3. 滑动找到"添加到主屏幕"
4. 点击"添加"
5. 现在桌面上有个图标了！

### Android (Chrome)
1. 打开网站
2. 点击右上角菜单 (⋮)
3. 点击"添加到主屏幕"
4. 点击"添加"
5. 桌面上出现图标！

---

## 🎨 自定义PWA图标

### 创建图标
需要两个尺寸的图标:
- `icon-192.png` (192x192像素)
- `icon-512.png` (512x512像素)

### 在线工具生成
1. 访问 https://favicon.io/
2. 上传你的图片
3. 下载生成的图标
4. 将 `icon-192.png` 和 `icon-512.png` 放到 `public/` 文件夹

### 或使用Emoji图标
1. 访问 https://emojipedia.org/
2. 搜索 🍽️ 点餐相关emoji
3. 截图并调整尺寸
4. 保存为 `icon-192.png` 和 `icon-512.png`

---

## 🔄 更新部署

修改代码后重新部署:
```bash
git add .
git commit -m "更新内容描述"
git push
npm run deploy
```

---

## ✅ 部署检查清单

- [ ] 安装了 gh-pages
- [ ] 创建了GitHub仓库
- [ ] 配置了正确的base路径
- [ ] 推送代码到GitHub
- [ ] 运行了 npm run deploy
- [ ] 启用了GitHub Pages
- [ ] 添加了PWA图标
- [ ] 手机测试添加到桌面

---

## 🐛 常见问题

### 部署后页面空白？
检查 `vite.config.js` 的 `base` 配置是否正确

### 图标不显示？
确保 `public/` 文件夹有 `icon-192.png` 和 `icon-512.png`

### Service Worker不工作？
- 必须使用HTTPS (GitHub Pages自动提供)
- 检查浏览器控制台是否有错误

### 更新后没变化？
- 清除浏览器缓存
- 在Service Worker中增加版本号(如 v1 → v2)

---

**部署成功后，分享链接给朋友吧！** 🎉
